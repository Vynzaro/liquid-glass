{
  "name": "liquid-glass",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "node --test ../tests/*.test.cjs",
    "verify": "npm run build && npm test"
  },
  "devDependencies": {
    "@girs/gnome-shell": "^50.0.0",
    "typescript": "^6.0.3"
  }
}
